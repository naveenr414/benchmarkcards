{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "BenchmarkCard",
    "description": "Structured documentation for healthcare LLM benchmark assumptions (Raman et al., 2025).",
    "type": "object",
    "required": ["benchmark_name", "citation", "intended_use_case"],
    "additionalProperties": false,
    "properties": {
      "benchmark_name": { "type": "string" },
      "citation":        { "type": "string" },
      "version":         { "type": "string" },
  
      "intended_use_case": {
        "type": "object",
        "required": ["answer"],
        "additionalProperties": false,
        "properties": {
          "answer":              { "type": "string" },
          "deployment_context":  { "type": "string" }
        }
      },
  
      "task": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "query_authorship": {
            "$ref": "#/definitions/assumption_task"
          },
          "information_completeness": {
            "$ref": "#/definitions/assumption_task"
          },
          "interaction_type": {
            "allOf": [{ "$ref": "#/definitions/assumption_task" }],
            "properties": {
              "answer": {
                "enum": ["single-turn", "multi-turn", "both", "single-turn (single-shot)"]
              }
            }
          }
        }
      },
  
      "outcome": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "decision_mediation": {
            "$ref": "#/definitions/assumption_outcome"
          },
          "outcome_validity": {
            "$ref": "#/definitions/assumption_outcome"
          }
        }
      },
  
      "notes": { "type": "string" }
    },
  
    "definitions": {
      "assumption_task": {
        "type": "object",
        "required": ["question", "answer"],
        "additionalProperties": false,
        "properties": {
          "question":             { "type": "string" },
          "answer":               { "type": "string" },
          "holds_at_deployment":  { "type": "string" }
        }
      },
      "assumption_outcome": {
        "type": "object",
        "required": ["question", "answer"],
        "additionalProperties": false,
        "properties": {
          "question":             { "type": "string" },
          "answer":               { "type": "string" },
          "holds_at_deployment":  { "type": "string" }
        }
      }
    }
  }
  